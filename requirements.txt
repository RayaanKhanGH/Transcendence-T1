# AI / LLM / NLP
transformers>=4.35.0 # HuggingFace Transformers for NLP tasks
sentence-transformers>=2.2.2 # For embeddings
torch>=2.2.0 # Backend for Transformers
tensorflow>=2.14.0 # Optional, if you use TensorFlow models
accelerate>=0.23.0 # Optimizations for large models

# Vector database
pinecone-client>=2.3.1 # Pinecone vector DB integration

# Data storage / DB
psycopg2-binary>=2.9.9 # PostgreSQL adapter
sqlalchemy>=2.1.0 # ORM for easier DB operations

# Web scraping / HTTP requests
requests>=2.31.0
beautifulsoup4>=4.13.0
lxml>=4.9.3

# Utilities
pandas>=2.1.0 # Dataframes for analysis
numpy>=1.26.0
tqdm>=4.66.0 # Progress bars
python-dotenv>=1.0.0 # Load .env variables
cryptography>=41.0.2 # AES256, SHA512 encryption
pyjwt>=2.8.0 # Optional, for tokens
rich>=13.6.0 # Better CLI logs & visualization

# Async / concurrent
aiohttp>=3.9.0
asyncio>=3.1.0 # Built-in, may not be necessary in reqs.txt

# Optional dev / AI assistance
jupyterlab>=4.1.0 # For prototyping and experimenting
black>=23.12.0 # Formatting
flake8>=6.1.0 # Linting